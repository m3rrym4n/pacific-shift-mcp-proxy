name: "Pacific Shift MCP Proxy"
description: "Multi-route MCP webhook proxy — exposes multiple LAN MCP servers through Nabu Casa"
version: "1.0.0"
slug: "pacific_shift_mcp_proxy"
init: false
arch:
  - aarch64
  - amd64
homeassistant_api: true
auth_api: false
map:
  - config:rw
options:
  routes:
    - name: "example"
      upstream: "http://192.168.1.68:6001"
  remote_url: ""
  debug_logging: false
schema:
  routes:
    - name: str
      upstream: str
  remote_url: str
  debug_logging: bool
